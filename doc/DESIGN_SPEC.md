
## Design System

- Typography: ベース本文は `0.95rem`、補助ラベルは `0.72rem`、画面タイトルは小さめの uppercase で運用する。
- Accent Color: 主アクセントは `#8bd3ff`、補助アクセントは `#ff8a5b` と `#f7c948` を3Dオブジェクトや状態表示に使う。
- Tone: 画面全体は奥行きが見えるクールトーンを基準にし、背景は暗め、UI面は半透明または淡色で整理する。
- Spacing: 角丸は大きめ、余白は `12px`, `16px`, `24px` を基本ステップにする。
